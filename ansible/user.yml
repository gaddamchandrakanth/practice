---
- name: create a user
  hosts: all
  tasks:
    - name: create a user
      user:
        name: chandu
        password: chandu
        uid: 12345
        home: /tmp/passwd
        shell: /bin/bash
        
