---
- name: create a user 
  hosts: all
  tasks:
    - name: create a chandu user
      user:
        name: chandu
        password: 123

to execute the above playbook
command : 
          ansible-playbook playbook1.yml --ask-vault-pass
