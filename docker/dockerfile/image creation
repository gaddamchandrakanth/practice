COMMAND's:
     docker build -t myjenkins .
                 --no-cache          ( normally already executed will not do again 
                                             if we give this then it will execute once again  ) 
                                 ex: 
                                         1st time: sudo apt-get update in code 
                                          2nd time : 
                                              - docker will skip 
                                              - no cache gave then it will do process again
